# Copy to .env, fill real values. Never commit .env.
EXA_API_KEY=
MAPS_DATA_API_KEY=
LLM_API_KEY=
FORM_ENDPOINT=
# Website lead-capture form (website/src/components/LeadForm.astro).
# Free key from https://web3forms.com/ — no account needed, just an email
# to receive it at. Form renders on metro pages only once this is set.
PUBLIC_WEB3FORMS_KEY=
